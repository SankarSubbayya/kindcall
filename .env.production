# Public Convex client URL (safe to commit — it's the client endpoint, not a secret).
# Lets any host (Vercel/Netlify/Pages) build the frontend against the live backend.
VITE_CONVEX_URL=https://dynamic-peccary-568.convex.cloud
